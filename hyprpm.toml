[repository]
name = "Hyprchroma"
authors = ["alexhulbert", "RomeoCavazza"]
description = "Per-window glass tint overlay for Hyprland"

[hyprchroma]
description = "Per-window glass tint overlay — v0.55.4 port"
output = "hyprchroma.so"
build = [
    "make all"
]
